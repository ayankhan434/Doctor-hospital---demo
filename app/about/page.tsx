import Image from 'next/image'; import {doctor} from '@/data/doctor'; import {Container} from '@/components/ui/Container'; import {SectionHeading} from '@/components/ui/SectionHeading'; import {Button} from '@/components/ui/Button';
export const metadata={title:'About the Doctor',description:`Learn about ${doctor.name}, ${doctor.specialization}.`};
export default function About(){return <section className="py-16 lg:py-24"><Container><SectionHeading eyebrow="About" title={`Meet ${doctor.name}.`} description="Replace this placeholder biography with verified professional information before launch."/><div className="mt-12 grid gap-12 lg:grid-cols-2 lg:items-center"><Image src={doctor.portrait} alt={`Portrait of ${doctor.name}`} width={900} height={1100} className="rounded-[2.5rem] object-cover"/><div><p className="text-lg leading-8 text-slate-600">{doctor.name} is a placeholder profile for this starter clinic website. The final copy should cover verified qualifications, professional experience, clinical philosophy and any relevant registrations or affiliations.</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><div className="rounded-3xl bg-mist p-5"><p className="text-xs text-slate-500">Qualification</p><p className="mt-1 font-semibold">{doctor.qualification}</p></div><div className="rounded-3xl bg-mist p-5"><p className="text-xs text-slate-500">Experience</p><p className="mt-1 font-semibold">{doctor.experience}</p></div></div><Button href="/appointment" className="mt-8">Book a Consultation</Button></div></div></Container></section>}
