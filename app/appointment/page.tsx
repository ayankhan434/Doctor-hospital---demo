import {Container} from '@/components/ui/Container'; import {SectionHeading} from '@/components/ui/SectionHeading'; import {AppointmentForm} from '@/components/AppointmentForm'; import {doctor} from '@/data/doctor';
export const metadata={title:'Book an Appointment',description:'Request a medical consultation appointment.'};
export default function Appointment(){return <section className="bg-mist py-16 lg:py-24"><Container><div className="grid gap-10 lg:grid-cols-[.7fr_1.3fr] lg:items-start"><div><SectionHeading eyebrow="Appointment" title="Tell us how we can help." description="Submit a request and the clinic can follow up with you. Replace the demo endpoint with your real workflow before launch."/><div className="mt-8 rounded-3xl bg-ink p-6 text-sm leading-7 text-slate-300"><p className="font-semibold text-white">{doctor.clinicName}</p><p className="mt-2">{doctor.address}</p><p>{doctor.phone}</p><p>{doctor.hours}</p></div></div><AppointmentForm/></div></Container></section>}
