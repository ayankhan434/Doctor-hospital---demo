import {Container} from '@/components/ui/Container'; import {SectionHeading} from '@/components/ui/SectionHeading'; import {doctor} from '@/data/doctor'; import {Button} from '@/components/ui/Button';
export const metadata={title:'Contact',description:'Contact the clinic for appointments and enquiries.'};
export default function Contact(){return <section className="py-16 lg:py-24"><Container><SectionHeading eyebrow="Contact" title="Let’s start a conversation." description="Use the verified contact details for the real clinic before publishing."/><div className="mt-12 grid gap-5 md:grid-cols-3"><div className="rounded-3xl border border-slate-200 p-7"><p className="text-xs uppercase tracking-wider text-slate-500">Call</p><a href={`tel:${doctor.phone.replace(/\s/g,'')}`} className="mt-3 block text-lg font-semibold">{doctor.phone}</a></div><div className="rounded-3xl border border-slate-200 p-7"><p className="text-xs uppercase tracking-wider text-slate-500">Email</p><a href={`mailto:${doctor.email}`} className="mt-3 block text-lg font-semibold break-all">{doctor.email}</a></div><div className="rounded-3xl border border-slate-200 p-7"><p className="text-xs uppercase tracking-wider text-slate-500">Visit</p><p className="mt-3 text-lg font-semibold">{doctor.address}</p></div></div><Button href="/appointment" className="mt-8">Book Appointment</Button></Container></section>}
