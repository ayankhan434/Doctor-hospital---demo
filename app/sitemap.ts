import type {MetadataRoute} from 'next'; import {treatments} from '@/data/treatments'; import {absoluteUrl} from '@/lib/utils'; export default function sitemap():MetadataRoute.Sitemap{const staticPaths=['','/about','/treatments','/appointment','/contact','/privacy-policy','/terms'];return [...staticPaths.map(path=>({url:absoluteUrl(path),lastModified:new Date()})),...treatments.map(t=>({url:absoluteUrl(`/treatments/${t.slug}`),lastModified:new Date()}))]}
