'use client'; import {useState} from 'react'; import {useForm} from 'react-hook-form'; import {z} from 'zod'; import {zodResolver} from '@hookform/resolvers/zod';
const schema=z.object({name:z.string().min(2,'Please enter your full name'),phone:z.string().min(10,'Please enter a valid phone number'),email:z.string().email('Please enter a valid email'),date:z.string().min(1,'Choose a preferred date'),time:z.string().min(1,'Choose a preferred time'),concern:z.string().min(2,'Please enter your concern'),message:z.string().optional()}); type FormData=z.infer<typeof schema>;
export function AppointmentForm(){const [status,setStatus]=useState<'idle'|'loading'|'success'|'error'>('idle');const {register,handleSubmit,formState:{errors}}=useForm<FormData>({resolver:zodResolver(schema)});const onSubmit=async(data:FormData)=>{setStatus('loading');try{const r=await fetch('/api/enquiry',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw new Error();setStatus('success')}catch{setStatus('error')}};const field=(name:keyof FormData,label:string,type='text')=><label className="block"><span className="text-sm font-semibold text-ink">{label}</span><input type={type} {...register(name)} className="mt-2 w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm outline-none transition focus:border-teal focus:ring-4 focus:ring-teal/10" />{errors[name]&&<span className="mt-1 block text-xs text-red-600">{errors[name]?.message}</span>}</label>;return <form onSubmit={handleSubmit(onSubmit)} className="rounded-[2rem] border border-slate-200 bg-white p-5 shadow-soft sm:p-8"><div className="grid gap-5 sm:grid-cols-2">{field('name','Full Name')}{field('phone','Phone Number','tel')}{field('email','Email','email')}{field('date','Preferred Date','date')}{field('time','Preferred Time','time')}{field('concern','Concern / Disease')}</div><label className="mt-5 block"><span className="text-sm font-semibold text-ink">Message</span><textarea {...register('message')} rows={5} className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-teal focus:ring-4 focus:ring-teal/10"/></label><button disabled={status==='loading'} className="mt-6 w-full rounded-full bg-ink px-5 py-3.5 text-sm font-semibold text-white transition hover:bg-navy disabled:opacity-60">{status==='loading'?'Sending…':'Send Appointment Request'}</button>{status==='success'&&<p className="mt-4 rounded-2xl bg-teal/10 p-4 text-sm text-teal">Thank you. Your request has been received by this demo endpoint.</p>}{status==='error'&&<p className="mt-4 rounded-2xl bg-red-50 p-4 text-sm text-red-700">Something went wrong. Please try again or call the clinic.</p>}<p className="mt-4 text-xs leading-5 text-slate-500">This form is a placeholder implementation. Connect it to your email, WhatsApp, CRM or database before production.</p></form>}
