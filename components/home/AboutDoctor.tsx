import Image from 'next/image'; import {CheckCircle2} from 'lucide-react'; import {doctor} from '@/data/doctor'; import {Button} from '@/components/ui/Button'; import {Container} from '@/components/ui/Container'; import {SectionHeading} from '@/components/ui/SectionHeading';
export function AboutDoctor(){return <section className="py-20 lg:py-28"><Container className="grid items-center gap-12 lg:grid-cols-2"><div className="relative"><div className="absolute -bottom-5 -left-5 h-32 w-32 rounded-3xl bg-aqua"/><div className="relative overflow-hidden rounded-[2.5rem]"><Image src={doctor.portrait} alt={`Dr. ${doctor.name} in consultation`} width={900} height={1100} className="aspect-[4/5] object-cover"/></div></div><div><SectionHeading eyebrow="Meet the doctor" title={`Care built around listening first.`} description={`${doctor.name} brings ${doctor.experience.toLowerCase()} of clinical experience to patient conversations, with an emphasis on understandable guidance and appropriate follow-up.`}/><div className="mt-7 grid gap-3 sm:grid-cols-2">{['Clear communication','Personalized consultation','Evidence-informed decisions','Thoughtful follow-up'].map(x=><div key={x} className="flex items-center gap-2 text-sm font-medium text-slate-700"><CheckCircle2 className="h-4 w-4 text-teal"/>{x}</div>)}</div><Button href="/about" variant="secondary" className="mt-8">Know More About Doctor</Button></div></Container></section>}
