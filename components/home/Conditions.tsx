import {ArrowRight} from 'lucide-react'; import {treatments} from '@/data/treatments'; import {Container} from '@/components/ui/Container'; import {SectionHeading} from '@/components/ui/SectionHeading'; import {TreatmentCard} from '@/components/ui/TreatmentCard'; import {Button} from '@/components/ui/Button';
export function Conditions(){return <section className="bg-mist py-20 lg:py-28"><Container><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><SectionHeading eyebrow="Conditions & concerns" title="Care for a range of common health needs." description="Explore the editable treatment library below. Only conditions appropriate to the real doctor's scope should be published."/><div className="shrink-0 rounded-2xl border border-teal/20 bg-white px-5 py-4"><p className="text-3xl font-bold text-ink">{treatments.length}+</p><p className="text-xs text-slate-500">Conditions in this demo</p></div></div><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{treatments.map(t=><TreatmentCard key={t.slug} item={t}/>)}</div><div className="mt-8 text-center"><Button href="/treatments" variant="secondary">View All Conditions <ArrowRight className="ml-2 h-4 w-4"/></Button></div></Container></section>}
