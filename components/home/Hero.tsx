'use client'; import Image from 'next/image'; import {motion} from 'framer-motion'; import {ArrowRight,PhoneCall,ShieldCheck} from 'lucide-react'; import {doctor} from '@/data/doctor'; import {Button} from '@/components/ui/Button';
export function Hero(){return <section className="overflow-hidden bg-gradient-to-b from-mist to-white"><div className="mx-auto grid max-w-7xl items-center gap-12 px-5 pb-20 pt-12 sm:px-6 lg:grid-cols-[1.05fr_.95fr] lg:px-8 lg:pb-28 lg:pt-20"><motion.div initial={{opacity:0,x:-25}} animate={{opacity:1,x:0}} transition={{duration:.6}}><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-teal/20 bg-white px-3 py-2 text-xs font-semibold text-teal shadow-sm"><ShieldCheck className="h-4 w-4"/> Trusted Medical Care</div><h1 className="max-w-3xl font-display text-5xl font-semibold leading-[1.02] tracking-[-.04em] text-ink sm:text-6xl lg:text-7xl">Thoughtful care for your <span className="text-teal">everyday health.</span></h1><p className="mt-6 max-w-xl text-base leading-7 text-slate-600 sm:text-lg">Meet {doctor.name}, {doctor.specialization.toLowerCase()} focused on clear communication, careful assessment and personalized care.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Button href="/appointment">Book an Appointment <ArrowRight className="ml-2 h-4 w-4"/></Button><Button href={`tel:${doctor.phone.replace(/\s/g,'')}`} variant="secondary"><PhoneCall className="mr-2 h-4 w-4"/> Call Clinic</Button></div><div className="mt-9 flex gap-8 border-t border-slate-200 pt-6"><div><p className="text-2xl font-bold text-ink">{doctor.experience}</p><p className="text-xs text-slate-500">Experience</p></div><div><p className="text-2xl font-bold text-ink">{doctor.stats.patients}</p><p className="text-xs text-slate-500">Placeholder consultations</p></div><div><p className="text-2xl font-bold text-ink">{doctor.stats.conditions}</p><p className="text-xs text-slate-500">Conditions*</p></div></div></motion.div><motion.div initial={{opacity:0,scale:.96}} animate={{opacity:1,scale:1}} transition={{duration:.7,delay:.1}} className="relative mx-auto w-full max-w-xl"><div className="absolute -inset-4 rounded-[3rem] bg-teal/10 blur-2xl"/><div className="relative overflow-hidden rounded-[2.5rem] bg-white p-3 shadow-soft"><div className="relative aspect-[4/5] overflow-hidden rounded-[2rem]"><Image src={doctor.portrait} alt={`Portrait of ${doctor.name}`} fill priority sizes="(max-width:1024px) 90vw, 45vw" className="object-cover"/><div className="absolute inset-x-5 bottom-5 rounded-2xl border border-white/40 bg-white/90 p-4 backdrop-blur-md"><p className="text-sm font-semibold text-ink">{doctor.name}</p><p className="mt-1 text-xs text-slate-500">{doctor.qualification}</p></div></div></div></motion.div></div></section>}
