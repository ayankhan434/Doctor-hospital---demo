'use client';
import Link from 'next/link'; import {useState} from 'react'; import {Menu,X} from 'lucide-react'; import {Button} from '@/components/ui/Button';
export function Navbar(){const [open,setOpen]=useState(false);const links=[['About','/about'],['Treatments','/treatments'],['Why Choose Us','#why'],['Contact','/contact']];return <header className="sticky top-0 z-50 border-b border-slate-200/70 bg-white/90 backdrop-blur-xl"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 sm:px-6 lg:px-8"><Link href="/" className="flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-2xl bg-ink text-lg font-bold text-white">+</span><span><span className="block text-sm font-bold tracking-tight text-ink">Mehta Care</span><span className="block text-[11px] text-slate-500">Clinic & Internal Medicine</span></span></Link><nav className="hidden items-center gap-7 lg:flex">{links.map(([label,href])=><Link key={label} href={href} className="text-sm font-medium text-slate-600 transition hover:text-ink">{label}</Link>)}<Button href="/appointment">Book Appointment</Button></nav><button className="rounded-xl p-2 lg:hidden" aria-label="Toggle menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t border-slate-200 bg-white px-5 py-4 lg:hidden">{links.map(([label,href])=><Link key={label} href={href} onClick={()=>setOpen(false)} className="block rounded-xl px-3 py-3 text-sm font-medium text-slate-700">{label}</Link>)}<Button href="/appointment" className="mt-2 w-full">Book Appointment</Button></div>}</header>}
