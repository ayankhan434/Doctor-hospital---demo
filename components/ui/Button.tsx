import Link from 'next/link';
export function Button({href,children,variant='primary',className=''}:{href?:string;children:React.ReactNode;variant?:'primary'|'secondary'|'ghost';className?:string}){const c=`inline-flex items-center justify-center rounded-full px-5 py-3 text-sm font-semibold transition duration-300 focus:outline-none focus:ring-2 focus:ring-teal/40 ${variant==='primary'?'bg-ink text-white hover:-translate-y-0.5 hover:bg-navy shadow-lg shadow-ink/10':variant==='secondary'?'border border-slate-200 bg-white text-ink hover:-translate-y-0.5 hover:border-teal/40':'text-ink hover:bg-slate-100'} ${className}`; return href?<Link href={href} className={c}>{children}</Link>:<button className={c}>{children}</button>}
