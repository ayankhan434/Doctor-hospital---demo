import type { Metadata } from 'next';
import { doctor } from '@/data/doctor';
export const siteMetadata: Metadata = { metadataBase:new URL(process.env.NEXT_PUBLIC_SITE_URL || 'https://example.com'), title:{default:`${doctor.name} | ${doctor.specialization} in ${doctor.location.split(',')[0]}`,template:`%s | ${doctor.name}`}, description:`Professional ${doctor.specialization.toLowerCase()} services at ${doctor.clinicName} in ${doctor.location}.`, alternates:{canonical:'/'}, openGraph:{type:'website',siteName:doctor.clinicName,title:`${doctor.name} | ${doctor.specialization}`,description:`Patient-centered medical consultations at ${doctor.clinicName}.`,images:[doctor.portrait]}, twitter:{card:'summary_large_image',title:`${doctor.name} | ${doctor.specialization}`,description:`Patient-centered medical care at ${doctor.clinicName}.`,images:[doctor.portrait]}, robots:{index:true,follow:true} };
